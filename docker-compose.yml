---
version: "3.9"
services:
  catalog-service:
    build: .
    container_name: catalog-service
    restart: always
    command: ./server
    ports:
      - 8081:8081
    env_file:
      - .env
    networks:
      - ecommerce-network

networks:
  ecommerce-network:
    external: true
